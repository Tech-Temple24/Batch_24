let number = [1, 2, 3, 4, 5, 6];

//I want to add 45 to the array



//pop method
number.pop();
number.pop();
number.pop();

//It will always remove from the end, no need to tell what to remove

console.log(number);


let arr = []

arr.push(number);

console.log(arr);
